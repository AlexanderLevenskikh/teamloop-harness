# YourAITeam 0.4.1 — canonical naming pass

## Decision

`YOUR_AI_TEAM_CANONICAL_NAME_PASS`

## Changed

- renamed the repository/package root to `your-ai-team`;
- renamed the public CLI from `your-team` to `your-ai-team`;
- renamed the OpenCode command to `/your-ai-team`;
- renamed `TEAMLOOP.md` to `RUNTIME.md`;
- renamed the composition module to `your_ai_team.py`;
- changed package output to `your-ai-team-<version>.zip`;
- changed public schema provenance to `your-ai-team.dev`;
- replaced former product branding in active prose, prompts, descriptions, plans, and tests;
- added a branding validator and migration guide.

## Compatibility boundary

Persisted `.teamloop/` workspaces, `TEAMLOOP_*` test controls, and internal runtime module names remain compatible technical identifiers in 0.4.x. They are not presented as product branding.
