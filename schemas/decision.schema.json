{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Decision",
  "description": "A recorded architectural or design decision",
  "type": "object",
  "required": ["schemaVersion", "decisionId", "title", "description", "status", "createdAtUtc"],
  "properties": {
    "schemaVersion": { "type": "integer", "const": 1 },
    "decisionId": { "type": "string", "pattern": "^decision-[a-zA-Z0-9-]+$" },
    "title": { "type": "string", "minLength": 1 },
    "description": { "type": "string", "minLength": 1 },
    "status": {
      "type": "string",
      "enum": ["ACTIVE", "DEPRECATED", "REJECTED", "SUPERSEDED"]
    },
    "evidenceIds": {
      "type": "array",
      "items": { "type": "string", "pattern": "^evidence-[a-zA-Z0-9-]+$" },
      "minItems": 1
    },
    "alternatives": {
      "type": "array",
      "items": { "type": "string" }
    },
    "createdAtUtc": { "type": "string", "format": "date-time" },
    "deprecatedAtUtc": { "type": "string", "format": "date-time" },
    "supersededBy": { "type": "string", "pattern": "^decision-[a-zA-Z0-9-]+$" }
  },
  "additionalProperties": false
}
